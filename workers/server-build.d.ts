// The React Router Vite plugin provides this virtual module at build time; the
// bare declaration lets the Worker import it without a type error.
declare module "virtual:react-router/server-build";
